# Chave da API da Anthropic — necessária só para o gerador de preview com IA
# ("E se...", em src/app/api/preview/route.ts). Pegue a sua em console.anthropic.com.
# Copie este arquivo para .env.local (que já está no .gitignore) e cole a chave.
ANTHROPIC_API_KEY=
